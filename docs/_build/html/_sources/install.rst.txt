Install
=======

The ``eph`` package is tested using python3.4.

Dependencies
------------

The ``eph`` package depends on ``requests`` (visit http://docs.python-requests.org/en/master/ for more info).

Using pip::

	pip install -r requirements.txt

